/docs

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...but keep the checked-in template
!.env.local.example

# typescript
*.tsbuildinfo
next-env.d.ts

# embed build artifact
/public/embed.js

# firebase emulator
firebase-debug.log
firestore-debug.log
ui-debug.log
/.firestore-data
# throwaway emulator key (generate with `npm run gen:key`); template is tracked
/firebase-emulator-sa.json
